// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider   = "prisma-client-js"
  // Prisma v7 defaults to the new WASM "client" engine which requires an
  // adapter or accelerateUrl. Use the traditional binary engine for
  // standard PostgreSQL connectivity via DATABASE_URL.
  engineType = "binary"
}

datasource db {
  provider = "postgresql"
}

enum Role {
  ADMIN
  USER
  DEV
}

model User {
  id        String    @id @default(cuid())
  email     String    @unique
  password  String
  role      Role
  projects  Project[]
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model Project {
  id          String       @id @default(cuid())
  name        String
  ownerId     String
  owner       User         @relation(fields: [ownerId], references: [id])
  status      String
  deployments Deployment[]
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
}

model Deployment {
  id        String   @id @default(cuid())
  projectId String
  project   Project  @relation(fields: [projectId], references: [id])
  status    String
  logs      String   @default("")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
